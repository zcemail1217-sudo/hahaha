# AGENTS.md

本项目默认协作规则：

1. 每次完成一个明确的代码修改、功能实现或问题修复后，先检查 `git status`。
2. 如能运行测试或构建，应先验证。
3. 如果修改已完成且没有明显风险，自动执行：
   - `git add .`
   - `git commit -m "简短说明本次修改"`
   - `git push`
4. 如果存在编译失败、测试失败、疑似敏感信息、未完成试验代码，或工作区里有不属于本次任务的改动，先向用户确认，不要自动提交推送。
5. 不要提交 `bin/`、`obj/`、`.vs/`、`artifacts/` 等构建产物。
