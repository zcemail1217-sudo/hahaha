<UserControl x:Class="VisionStation.Client.Views.HistoryRecordsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid Background="{StaticResource WorkbenchBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="58" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Style="{StaticResource IconTextStyle}" Text="&#xE81C;" Width="30" />
                <StackPanel Margin="8,0,0,0">
                    <TextBlock Text="历史记录" FontSize="18" FontWeight="SemiBold" />
                    <TextBlock Text="查看最近 100 条检测记录和存入表格的结果字段。"
                               Foreground="{StaticResource TextSecondaryBrush}" />
                </StackPanel>
            </StackPanel>
            <Button Command="{Binding ReloadCommand}"
                    Style="{StaticResource ActionButtonStyle}"
                    HorizontalAlignment="Right">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Style="{StaticResource IconTextStyle}" Text="&#xE72C;" Width="20" FontSize="14" />
                    <TextBlock Text="刷新" Margin="6,0,0,0" />
                </StackPanel>
            </Button>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.8*" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource PanelBorderStyle}" Margin="0,0,12,0">
                <DataGrid ItemsSource="{Binding Records}"
                          SelectedItem="{Binding SelectedRecord}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="时间" Binding="{Binding Time}" Width="150" />
                        <DataGridTextColumn Header="配方" Binding="{Binding RecipeName}" Width="170" />
                        <DataGridTextColumn Header="结果" Binding="{Binding Outcome}" Width="80" />
                        <DataGridTextColumn Header="节拍" Binding="{Binding CycleTime}" Width="90" />
                        <DataGridTextColumn Header="条码" Binding="{Binding Barcode}" Width="160" />
                        <DataGridTextColumn Header="结果字段" Binding="{Binding ResultSummary}" Width="260" />
                        <DataGridTextColumn Header="原图" Binding="{Binding OriginalImagePath}" Width="*" />
                        <DataGridTextColumn Header="结果图" Binding="{Binding ResultImagePath}" Width="*" />
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource PanelBorderStyle}">
                <DockPanel>
                    <StackPanel DockPanel.Dock="Top" Margin="0,0,0,10">
                        <TextBlock Text="结果表明细" Style="{StaticResource SectionTitleStyle}" />
                        <TextBlock Text="{Binding SelectedRecord.ResultSummary}"
                                   Margin="0,6,0,0"
                                   TextWrapping="Wrap"
                                   Foreground="{StaticResource TextSecondaryBrush}" />
                    </StackPanel>

                    <ItemsControl ItemsSource="{Binding SelectedResultFields}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Margin="0,0,0,8"
                                        Padding="10,8"
                                        Background="{StaticResource SurfaceAltBrush}"
                                        BorderBrush="{StaticResource AccentLineBrush}"
                                        BorderThickness="1"
                                        CornerRadius="5">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="1.1*" />
                                            <ColumnDefinition Width="1.4*" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding Key}" FontWeight="SemiBold" />
                                        <TextBlock Grid.Column="1"
                                                   Text="{Binding Value}"
                                                   TextWrapping="Wrap"
                                                   Foreground="{StaticResource AccentBrush}" />
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </DockPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
